# Mined additions for `family-cell-seeding` — PROPOSALS, pending Claude integration

_Generated by scripts/mine_bucket.py. Each section is one protocol's material novelty for Claude to fold into the real skill, then validate (SOP §7). This file is NOT a skill._

<!-- batch run 2026-05-26 -->
### sci-lucif-assay3
_adds supernatant removal to waste, PBS wash cycle, and compound treatment dispensing with multi-liquid-class handling_

- `WASTE_TROUGH` role (`Trough, 100ml SBS`) for supernatant and wash removal via `head.aspirate(..., "MEDIUM_VOL_UL", liquid_class="LIQUID_CLASS_MEDIUM")`
- Scalar volumes: `PBS_WASH_VOLUME_UL`, `TREATMENT_VOLUME_UL`; distinct liquid classes `"LIQUID_CLASS_PBS"`, `"LIQUID_CLASS_TREATMENT"`
- Native `wt.loop(times=TOTAL_COLS, loop_variable='col')` with column-wise aspirate-to-waste → dispense-PBS → aspirate-to-waste → dispense-treatment sequence
- Note: Opentrons `air_gap(20)` and custom flow rates (`rate=...`) are dropped per G2 scalar-volume/flow-rate limits; handled via standard liquid class defaults

