# Mined additions for `family-cherrypicking` — PROPOSALS, pending Claude integration

_Generated by scripts/mine_bucket.py. Each section is one protocol's material novelty for Claude to fold into the real skill, then validate (SOP §7). This file is NOT a skill._

<!-- batch run 2026-05-26 -->
### cherrypicking-sick-kids
_adds in-tip dilution aspiration sequence and post-dispense destination mixing_

- `DILUENT_VOLUME_UL` variable; single-tip sequence: aspirate diluent → aspirate sample → dispense combined volume (air gaps dropped per scope)
- Post-dispense mixing step in destination well using `head.mix()` with derived mix volume/cycles

### 2bba96
_adds buffer pre-fill phase and volume-threshold pipette selection logic_

- Buffer pre-fill phase loop distributing fixed buffer volumes to dest wells before sample transfers
- Volume-threshold pipette routing: split pick list into ≤20 µL and >20 µL groups, each mapped to appropriate `wt.liha` configuration or separate head instances

