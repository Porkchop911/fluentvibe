# Mined additions for `family-reagent-distribution` — PROPOSALS, pending Claude integration

_Generated by scripts/mine_bucket.py. Each section is one protocol's material novelty for Claude to fold into the real skill, then validate (SOP §7). This file is NOT a skill._

<!-- batch run 2026-05-26 -->
### 4a0be6
_adds tube-rack destination labware role and sequential well-by-well distribution loop_

- Labware whitelist addition: `TubeRack12/24/48` (Sarstedt screw-cap tubes) resolved via `fluentvibe/catalog` (`find_components`) and added to `generation.yaml` `lab_scope.labware`.
- Structural variant: Single-channel (`wt.liha`) sequential distribution loop across multiple tube racks, using native `wt.loop(times=N, loop_variable='well_idx')` with explicit well addressing instead of column/plate patterns.
- Capability gap: Dynamic source liquid-level tracking (Opentrons `.bottom(z=...)` / custom height class) is dropped per G2 scalar-volume limits; model as uniform aspiration with standard reservoir fill calculation (`total_wells × DISPENSE_VOLUME_UL × 1.1`).

<!-- batch run 2026-05-26 -->
### sci-lucif-assay2
_partitioned multi-mix column distribution from tube rack sources_

- Variable `NUM_MASTERMIX` (choice 1/2/3/4/6/12) defining reagent partitions; derive `COLS_PER_MIX = 12 // NUM_MASTERMIX`.
- Source labware role: multi-well rack (`TubeRack` or `Plate96`) holding distinct master mixes, replacing single reservoir.
- Loop structure: outer `wt.loop(times=NUM_MASTERMIX)` over source wells, inner column dispense with dynamic `well_offset` targeting contiguous destination columns per mix.
- Technique: partitioned column-wise distribution with tip drop/renewal between master mixes to prevent cross-contamination.

