# Mined additions for `family-pcr-setup` — PROPOSALS, pending Claude integration

_Generated by scripts/mine_bucket.py. Each section is one protocol's material novelty for Claude to fold into the real skill, then validate (SOP §7). This file is NOT a skill._

<!-- batch run 2026-05-26 -->
### kapa-library-quant-48x
_adds stepwise sample dilution, standard curve dispensing, and 96/384 format switching with multi-replicate volume splitting_

- `PLATE_FORMAT` (`96`|`384`) variable driving conditional well offsets, replicate counts (`NUM_REPLICATES`), and per-well volumes via derived calculations
- Stepwise dilution variables (`DILUTION_VOL_1_UL`, `DILUTION_VOL_2_UL`, `BUFFER_VOL_UL`) for 1:50 → 1:20 sample prep before reaction assembly
- Standard curve setup: `STANDARD_VOLUME_UL` + column-wise `wt.loop` to dispense calibration standards into leading wells
- Labware roles: `Plate384` (catalog-resolved), deep-well/reservoir for dilution buffer, source plate for standards
- Multi-replicate dispensing: sequential `wt.loop(times=NUM_REPLICATES)` to split total reaction volume into smaller aliquots per well

