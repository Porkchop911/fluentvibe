# Mined additions for `family-bead-cleanup-spri` — PROPOSALS, pending Claude integration

_Generated by scripts/mine_bucket.py. Each section is one protocol's material novelty for Claude to fold into the real skill, then validate (SOP §7). This file is NOT a skill._

<!-- batch run 2026-05-26 -->
### zymo_quick-rna_flex_96-cells
_adds lysis/DNase/neutralization steps and multiple distinct wash buffer roles to the bead workflow_

- Declare `LYSIS_VOLUME_UL`, `DNASE_TREATMENT_VOLUME_UL`, `NEUTRALIZATION_VOLUME_UL` as scalar variables; pass by name to dispense/mix calls
- Replace single ethanol role with per-wash liquid classes (`LIQUID_CLASS_WASH1`, `LIQUID_CLASS_WASH2`, etc.) for distinct buffer handling
- Structural variant: pre-bead lysis/enzymatic treatment groups modeled as sequential dispense/mix/wait steps before bead binding

### immunoprecipitation-by-dynabeads-on-flex-96ch
_adds antibody binding phase, extended incubation variable, and protein-specific elution/heating step_

- Declare `ANTIBODY_VOLUME_UL` and `BINDING_INCUBATION_SECONDS`; pass by name for extended wait after sample+Ab addition
- Add `LIQUID_CLASS_ANTIBODY` role; model heat-denaturation elution as `wt.wait(duration_seconds=...)` + `wt.add_comment(...)`
- Structural variant: bead pre-wash -> sample+Ab incubation -> magnet separation -> washes -> elute (protein capture workflow)

<!-- batch run 2026-05-26 -->
### magnesil_rna_ot2
_on-bead enzymatic treatment (DNase I digestion + neutralization) between wash cycles_

- On-bead enzymatic processing group inserted between wash cycles: dispense `"DNASE_VOLUME_UL"` (`role="enzyme"`), mix off-magnet, `wt.wait(duration_seconds="DNASE_INCUBATION_SECONDS")`, then dispense `"NEUTRALIZATION_VOLUME_UL"` (`role="stop_buffer"`) and mix.
- Declare scalar volume variables for enzyme/neutralizer steps; update dependent supernatant aspirate calculations to include these added reagent volumes before the subsequent wash step.

<!-- batch run 2026-05-26 -->
### immunoprecipitation-by-dynabeads-on-flex-reagents-in-15-ml-tubes
_affinity-capture workflow with antibody incubation step and conditional elution path_

- `BINDING_INCUBATION_SECONDS` + off-magnet mix before wash loop: model antibody capture as `head.mix(...)` then `wt.wait(duration_seconds="BINDING_INCUBATION_SECONDS")`.
- `ELUTION_PATH_CHOICE` (choice `"SDS_PAGE"`/`"COLD_STORE"`): drive a `wt.conditional` branch to either apply heat (`wt.wait` + comment) or transfer eluate to a cold plate.
- Map 15 mL tube rack to approved troughs (`25ml_short`/`100ml`) or add `NEST_15mL_TubeRack` to whitelist; declare per-reagent fill volumes using standard trough arithmetic.

### sp3_sample_prep
_multi-step bead-bound chemical processing workflow with dual-bead-type mixing and sequential reagent incubations_

- `BEAD_TYPE_A_RATIO`, `BEAD_TYPE_B_RATIO`: dual-bead slurry variables with derived combined volume (`BEAD_VOLUME_UL = round((A+B)*SAMPLE_VOLUME_UL, 1)`).
- Sequential on-plate processing loop: `wt.loop` over reagent steps (reduction, alkylation, digestion, labeling) dispensing from troughs, mixing off-magnet, and `wt.wait` incubations without intermediate transfers.
- `LIQUID_CLASS_ENZYME`, `LIQUID_CLASS_TMT`: role-specific classes for protease and labeling reagents; flag flow-rate/geometry gaps for viscous TMT buffers in prose.

