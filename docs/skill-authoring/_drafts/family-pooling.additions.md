# Mined additions for `family-pooling` — PROPOSALS, pending Claude integration

_Generated by scripts/mine_bucket.py. Each section is one protocol's material novelty for Claude to fold into the real skill, then validate (SOP §7). This file is NOT a skill._

<!-- batch run 2026-05-26 -->
### 019d4d_2
_worklist-driven per-well distinct volumes for normalization and pooling_

- `wt.worklist("pool_norm.csv")` integration for per-well variable volumes (DNA input, elution buffer, pool volume) and arbitrary source→dest mappings.
- Conditional mixing steps (`head.mix`) triggered per worklist row after normalization transfers.
- Tip strategy flags in worklist or fixed per-row pickup/drop to handle cross-contamination risks during sequential single-channel pooling.

### sci-idt-normalase
_adds CSV/worklist-driven arbitrary source-to-destination pooling (picklist consolidation)_

- `POOL_PICKLIST_CSV` — CSV defining explicit source-well → destination-well mappings for non-columnar consolidation
- Drive pooling iteration via `wt.worklist("POOL_PICKLIST_CSV")` instead of scalar `wt.loop` when targets are arbitrary or scattered
- Each record triggers a uniform-volume transfer: aspirate from mapped source, dispense to mapped dest, using `"POOL_VOLUME_UL"` and appropriate liquid class
- Tip management follows picklist density (fresh tip per transfer or conditional reuse based on adjacent records)

