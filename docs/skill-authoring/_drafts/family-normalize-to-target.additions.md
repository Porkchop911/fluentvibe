# Mined additions for `family-normalize-to-target` — PROPOSALS, pending Claude integration

_Generated by scripts/mine_bucket.py. Each section is one protocol's material novelty for Claude to fold into the real skill, then validate (SOP §7). This file is NOT a skill._

<!-- batch run 2026-05-26 -->
### ml-normalization-cdna-library
_worklist-driven per-well normalization with conditional dual-pipette volume routing_

- Worklist-driven per-well normalization: `wt.worklist("norm.csv")` carrying distinct `SAMPLE_VOL` / `DILUENT_VOL` per record.
- Conditional dual-head routing: switch between small/large LiHa based on volume threshold (`if vol > 20`), or unify under a single head with appropriate liquid classes.
- Per-well sequence: aspirate diluent → dispense dest → aspirate sample → dispense dest (worklist iteration replaces uniform scalar loops).

### bca_normalization_from_csv
_worklist-driven per-well BCA-style normalization (buffer-first, sample-add, mix)_

- Worklist-driven BCA normalization: `wt.worklist("bca.csv")` mapping dest well, buffer volume, and DNA/sample volume per row.
- Single-head workflow: pre-dispense buffer → aspirate sample → dispense sample → mix in destination well (`head.mix(...)`).
- Replace scalar uniform loops with worklist iteration to honor distinct per-well volumes; declare `"BUFFER_VOLUME_UL"` / `"SAMPLE_VOLUME_UL"` as worklist-bound variables.

